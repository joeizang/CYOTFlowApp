# Database Connection Strings
POSTGRES_CONN_STR=Host=localhost;Port=5433;Database=flowappdb;Username=postgres;Password=postgres;
SQLITE_CONN_STR="Data Source=./flowappdb.sqlite"

# Admin Configuration
ADMIN_PASSWORD="YourSecureAdminPassword"

# Email Settings
EMAIL_SENDER_EMAIL="your-email@gmail.com"
EMAIL_SENDER_PASSWORD="your-gmail-app-password"
EMAIL_SENDER_NAME="Your Application Name"

# Important Notes:
# 1. Copy this file to .env and update with your actual values
# 2. Never commit the .env file to source control
# 3. For Gmail, use an App Password (not your regular password):
#    - Enable 2FA on your Google Account
#    - Go to: Google Account > Security > App Passwords
#    - Generate an app password for "Mail"
#    - Use that password in EMAIL_SENDER_PASSWORD
